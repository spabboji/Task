public class products {
		    public static void task(){
        list<Product2> pro=new list<product2>();
           string des= +'%a';
		pro=[select name,description from product2 where description like '%a%'];
                for(product2 p:pro){
                    system.debug(p.description);
                }
            
    }
}
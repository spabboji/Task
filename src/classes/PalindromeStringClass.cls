public class PalindromeStringClass {
    
     public static void demo(string str){
         
       	 integer i;
         string[] names= str.splitByCharacterType(); 
         
         if(str != null && !''.equals(str)){
             
         for(i = 0;i<=names.size();i++){
           
          	system.debug(names[i] + ' \n'); 
             
   		}
	}
}
}